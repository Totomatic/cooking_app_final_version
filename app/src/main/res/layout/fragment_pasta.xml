<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.gpcorser.bitsandpizzas.MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:ems="10"
        android:id="@+id/minutesValue"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true" />

    <Space
        android:layout_below="@id/minutesValue"
        android:id="@+id/space2"
        android:layout_width="1dp"
        android:layout_height="@dimen/space_between_space"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:textSize="14dp"
        android:layout_below="@+id/space2"
        android:text="Start Timer"
        android:id="@+id/button_start_timer"
        android:onClick="startTimer" />

    <Space
        android:layout_below="@id/button_start_timer"
        android:id="@+id/space3"
        android:layout_width="1dp"
        android:layout_height="@dimen/space_between_space"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:textSize="14dp"
        android:layout_below="@+id/space3"
        android:text="Stop Timer"
        android:id="@+id/button_stop_timer"
        android:onClick="stopTimer" />

</RelativeLayout>
